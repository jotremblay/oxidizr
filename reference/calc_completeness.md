# Calculate Data Completeness Summary

Calculate completeness metrics for data columns.

## Usage

``` r
calc_completeness(data, variables = NULL)
```

## Arguments

- data:

  A data frame

- variables:

  Variables to check (NULL = all numeric)

## Value

A tibble with completeness metrics
